<view class="nav">
    <view s-for="item, index in navList" key={{index}}>
        <smt-icon name={{item.icon}} class="smt-icon"></smt-icon>
        <text>
            {{item.title}}
        </text>
    </view>
</view>